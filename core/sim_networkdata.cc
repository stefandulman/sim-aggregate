
#include "sim_networkdata.h"

sim_networkdata::sim_networkdata() {
  cout << "must fill " << __func__ << " with code." << endl;
}

sim_networkdata::~sim_networkdata() {
  cout << "must fill " << __func__ << " with code." << endl;
}
    
void sim_networkdata::setup() {
  cout << "must fill " << __func__ << " with code." << endl;
}
    
    
    
    
std::vector<double> sim_networkdata::get_data() {
  cout << "must fill " << __func__ << " with code." << endl;
  std::vector<double> v;
  return v;
}

double sim_networkdata::get_data(int _pos) {
  cout << "must fill " << __func__ << " with code." << endl;
  double d;
  return d;
}
    
void sim_networkdata::set_data(std::vector<double> _data) {
  cout << "must fill " << __func__ << " with code." << endl;
}

void sim_networkdata::set_data(int _pos, double _data) {
  cout << "must fill " << __func__ << " with code." << endl;
}
    
void sim_networkdata::reinit(void) {
  cout << "must fill " << __func__ << " with code." << endl;
}
    
void sim_networkdata::advance_time(int _nsteps) {
  cout << "must fill " << __func__ << " with code." << endl;
}

double sim_networkdata::value(int _pos) {
  cout << "must fill " << __func__ << " with code." << endl;
  double d;
  return d;
}

std::vector<double> vvalue(void) {
  cout << "must fill " << __func__ << " with code." << endl;
  std::vector<double> v;
  return v;
}


// int _power = 1, int _nsteps = -1
sim_networkdata sim_networkdata::sum(int _power, int _nsteps) {
  cout << "must fill " << __func__ << " with code." << endl;
  sim_networkdata x;
  return x;
}
    
sim_networkdata sim_networkdata::min(int _nsteps) {
  cout << "must fill " << __func__ << " with code." << endl;
  sim_networkdata x;
  return x;
}

sim_networkdata sim_networkdata::count(int _nsteps) {
  cout << "must fill " << __func__ << " with code." << endl;
  sim_networkdata x;
  return x;
}


